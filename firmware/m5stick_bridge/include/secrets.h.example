// Copy to secrets.h (gitignored) and fill in.
#pragma once

#define WIFI_SSID   "your-phone-hotspot-name"
#define WIFI_PASS   "your-phone-hotspot-password"

// bridge_server.py's host:port on the same hotspot network, e.g. the laptop's
// hotspot-assigned IP once WSL mirrored networking is on (see repo README).
#define WS_HOST     "192.168.1.100"
#define WS_PORT     8765
#define WS_PATH     "/"
