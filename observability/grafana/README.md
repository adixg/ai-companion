# observability/grafana

Not built yet. Phase 3 of `docs/deployment-architecture.md`, downstream of
`observability/prometheus/`: dashboards for per-service turn latency
(stt/agent/tts, end-to-end), and GPU memory/utilization per node, split by
`gpu-tier` label so the 1650 and 4060 are visible separately.
